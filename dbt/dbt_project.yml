name: urban_mobility
version: "1.0.0"
config-version: 2
profile: urban_mobility

model-paths:   ["models"]
seed-paths:    ["seeds"]
snapshot-paths:["snapshots"]
test-paths:    ["tests"]
macro-paths:   ["macros"]
target-path:   "target"
clean-targets: ["target", "dbt_packages"]

models:
  urban_mobility:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
